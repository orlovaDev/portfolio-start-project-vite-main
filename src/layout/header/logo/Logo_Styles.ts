import styled from "styled-components";

const Logo = styled.a`
  background: linear-gradient(135deg, #bad1f7 0%, #4a82da 100%);
  width: 40px;
  height: 40px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;

  svg {
    width: 40px;
    height: 40px;
    path {
      fill: #1F2626; 
    }
  }`
export const S = {
  Logo
}
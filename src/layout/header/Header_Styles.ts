import styled from "styled-components";

const Header = styled.header`
  background-color: rgba(1, 15, 35, 0.56);
  padding: 12px 0;
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  z-index: 99;
`

export const S = {
  Header
}
